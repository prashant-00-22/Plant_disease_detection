# TODO - Run Flask project without error

- [ ] Fix syntax error in `app.py` (misplaced docstring in `create_demo_model`).
- [ ] Verify project starts: `pip install -r requirements.txt` in a virtual environment.
- [ ] Run app: `python app.py` and confirm no startup exceptions.
- [ ] Smoke test routes: `/`, `/upload`, POST to `/result` with a sample image.

